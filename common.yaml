---
- name: Configure proxy servers
  hosts:
    - aws_proxy_nodes
    - vsphere_proxy_nodes
  become: yes

  tasks:
    - name: Install common packages
      package:
        name:
          - epel-release
          - haproxy
        state: present
